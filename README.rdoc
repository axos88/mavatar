= Mavatar

This project is basically a gravatar cahce.

Use it by mounting this engine inside any other rails application, for example by
adding the following line to /config/routes.rb:

    mount Mavatar::Engine, at: "/mavatar"
